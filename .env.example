# .env — Optional environment overrides
# Copy this file and rename to .env if needed.
# These values are OPTIONAL — defaults are set in config.py

# LLM Model (options: llama3, mistral, gemma2, phi3)
OLLAMA_MODEL=llama3

# Device (cpu or cuda)
EMBEDDING_DEVICE=cpu
NLI_DEVICE=-1
